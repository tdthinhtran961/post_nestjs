import { Controller } from '@nestjs/common';
import { UserService } from '../service/user.service';

@Controller('user')
export class UserController {
  
  private $;
  
  public function get${1/(.*)/:capitalize}() \{
    return $this->;
  }
}
